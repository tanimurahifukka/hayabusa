{
  "_comment": "Default dev config: echo Worker only. STT (whisper-cli) requires HAYABUSA_WHISPER_BIN + HAYABUSA_WHISPER_MODEL env to be set — use config/hayabusa.stt.dev.json when you want the dispatcher to lease stt.transcribe jobs.",
  "node": {
    "id": "edge-mac-studio-dev-01",
    "role": "sub",
    "capabilities": {
      "jobTypes": ["echo"]
    },
    "policy": {
      "maxConcurrentJobs": 2,
      "acceptPriority": [],
      "refuseWhenMemoryPressure": ["critical", "emergency"],
      "leaseBatchSize": 4,
      "pollIntervalSeconds": 2.0
    },
    "lease": {
      "enabled": true,
      "commandRoomBaseUrl": "http://localhost:3001",
      "authTokenEnv": "HAYABUSA_WORKER_TOKEN"
    }
  }
}
